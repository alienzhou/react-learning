// cSpell: ignore Alienact

import Component from './component';
import {render} from './dom';
import createElement from './element';
import createRef from './ref';
import createContext from './context';

const Alienact = {
    Component,
    render,
    createElement,
    createRef,
    createContext
};

export default Alienact;